[
  { "v": "1.0.0", "change": "Initial skill — drives the real daemon via the dev sandbox profile through scan/derive/place/render/dispatch; added smoke.sh (build→launch→screenshot→verify) and the gotchas.", "snag": "" },
  { "v": "1.1.0", "change": "Regenerated by running the app fresh: daemon now starts with polling OFF, so smoke.sh + 'drive by hand' now arm POST /polling before scanning. Fixed test count (96→168).", "snag": "Old smoke.sh never armed polling, so against today's daemon it scanned 0 PRs and FAILed — the stale skill was broken." }
]
