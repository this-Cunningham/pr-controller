EmptyState — shown when a section has no PRs.

```jsx
<EmptyState label="Nothing needs you right now." />
```

The open ensō (a circle with one quadrant absent) is the brand's emptiness motif — keep it; don't swap for an icon. Tone is calm and reassuring, never an error.
