ModeBadge — the safe ⇄ live agent-mode switch in the header.

```jsx
<ModeBadge mode={mode} onToggle={toggleMode} />
```

"safe" is calm and neutral (hollow dot); "live" uses the accent with a pulsing dot — the one place ambient motion is allowed, signalling the agent is acting autonomously.
