// Library version + shared constants.
//
// NOTE: the VERSION line below is a deliberate single-line conflict target used by
// the e2e rebase/merge-conflict scenarios — main and a PR branch each edit it.
export const VERSION = '1.0.0';

// Hard caps + defaults shared across modules.
export const MAX_ITEMS = 100;
export const DEFAULT_CURRENCY = 'USD';
