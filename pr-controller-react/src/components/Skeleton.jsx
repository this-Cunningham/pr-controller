// Re-exported from the design system (single source of truth). The DS Skeleton
// renders the same first-fetch caption + three fading placeholder cards and
// accepts optional { caption, count }; the app uses the defaults.
export { Skeleton as default } from '../design-system/components/feedback/Skeleton.jsx';
